# Storybook Konfigürasyonu

Bu doküman, projedeki `.storybook/main.ts` dosyasını açıklar. Storybook, UI bileşenlerini izole ortamda geliştirmek ve dokümante etmek için kullanılır.

## İçindekiler

1. [Konfigürasyon Özeti](#1-konfigürasyon-özeti)
2. [Stories Deseni](#2-stories-deseni)
3. [Addon'lar](#3-addonlar)
4. [Framework](#4-framework)
5. [İlgili Dosyalar](#5-ilgili-dosyalar)
6. [Komutlar](#6-komutlar)
7. [Hikâye Dosyası Örneği](#7-hikâye-dosyası-örneği)
8. [Referanslar](#8-referanslar)

---

## 1. Konfigürasyon Özeti

| Özellik       | Değer                                         |
| ------------- | --------------------------------------------- |
| **Dosya**     | `.storybook/main.ts`                          |
| **Framework** | Vue 3 + Vite                                  |
| **Stories**   | `*.mdx`, `*.stories.@(js\|jsx\|mjs\|ts\|tsx)` |

---

## 2. Stories Deseni

```ts
stories: [
  '../src/**/*.mdx',
  '../src/**/*.stories.@(js|jsx|mjs|ts|tsx)',
]
```

| Pattern                                        | Açıklama                    |
| ---------------------------------------------- | --------------------------- |
| `../src/**/*.mdx`                              | MDX dokümantasyon dosyaları |
| `../src/**/*.stories.@(js\|jsx\|mjs\|ts\|tsx)` | Hikâye dosyaları            |

---

## 3. Addon'lar

| Addon                      | Açıklama                        |
| -------------------------- | ------------------------------- |
| `@storybook/addon-a11y`    | Erişilebilirlik (a11y) kontrolü |
| `@storybook/addon-docs`    | Otomatik dokümantasyon          |
| `@storybook/addon-designs` | Tasarım araçları entegrasyonu   |

> **Not:** Chromatic görsel regression testleri `build:chromatic` komutu ile CLI üzerinden çalışır; addon olarak eklenmez.

### a11y (Erişilebilirlik) nedir?

**a11y** = accessibility (erişilebilirlik). Web uygulamalarının ekran okuyucu, klavye navigasyonu ve farklı yeteneklere sahip kullanıcılar tarafından kullanılabilir olmasını hedefler.

**addon-a11y ne yapar?** Storybook'ta her story'yi axe-core ile tarar; eksik `alt`, `aria-label`, kontrast, focus sırası gibi sorunları "Accessibility" panelinde listeler.

**Nasıl kullanılır?** `npm run storybook` çalıştırın, bir story seçin, alt panelde "Accessibility" sekmesine tıklayın. İhlaller varsa gösterilir; düzeltme önerileri sunulur.

---

## 4. Framework

```ts
framework: '@storybook/vue3-vite'
```

Vue 3 ve Vite ile Storybook çalışır. Vite build pipeline'ı kullanılır.

---

## 5. İlgili Dosyalar

| Dosya                   | Açıklama                                         |
| ----------------------- | ------------------------------------------------ |
| `.storybook/main.ts`    | Ana Storybook config                             |
| `.storybook/preview.ts` | Global decorator, parametreler                   |
| `.storybook/routes.ts`  | Vue Router rotaları (storybook-vue3-router için) |

---

## 6. Komutlar

| Komut                     | Açıklama                                          |
| ------------------------- | ------------------------------------------------- |
| `npm run storybook`       | Geliştirme sunucusu (port 6006)                   |
| `npm run build:storybook` | Statik Storybook build                            |
| `npm run build:chromatic` | Storybook build + Chromatic'e yükle (görsel test) |

---

## 7. Hikâye Dosyası Örneği

Projede mevcut örnek: `src/views/Dashboard/Ecommerce.stories.ts`

```ts
// src/views/Dashboard/Ecommerce.stories.ts
import type {
  Meta,
  StoryObj,
} from '@storybook/vue3'
import EcommerceView from './EcommerceView.vue'

const meta: Meta<typeof EcommerceView> = {
  component: EcommerceView,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen',
  },
}
export default meta

type Story = StoryObj<typeof EcommerceView>

export const Default: Story = {}
```

Mevcut story dosyaları: `src/views/Dashboard/Ecommerce.stories.ts`, `src/stories/Welcome.mdx`

---

## 8. Referanslar

- [Storybook for Vue 3](https://storybook.js.org/docs/vue/get-started/install)
- [Storybook Addon Designs](https://storybook.js.org/addons/@storybook/addon-designs)
- [Chromatic](https://www.chromatic.com/)
