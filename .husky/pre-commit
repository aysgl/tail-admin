#!/bin/sh
# Commit öncesi uyarı (IDE: View > Output > Git panelinde görünür)
echo ""
echo "⚠️  Commit öncesi hatırlatma: Lint, format ve build çalıştırdınız mı?"
echo "   npm run lint:fix  |  npm run build"
echo ""

# Lint-staged ve commit mesajı prepare-commit-msg'da çalışır
