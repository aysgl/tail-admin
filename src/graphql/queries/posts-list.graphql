# Post listesi (GraphQL Zero) — PageQueryOptions ile sayfalama

query Posts($options: PageQueryOptions) {
  posts(options: $options) {
    data {
      ...PostFields
    }
    meta {
      totalCount
    }
  }
}
