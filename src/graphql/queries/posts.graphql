# Post listesi (GraphQL Zero API)
# Sayfalama için PageQueryOptions kullanılır

query Posts($options: PageQueryOptions) {
  posts(options: $options) {
    data {
      id
      title
      body
    }
    meta {
      totalCount
    }
  }
}
